import React from 'react';

const Women = () => {
  let style = {
    width: '100%',
    height: '30rem',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '3rem',
    textAlign: 'center',
  };
  return <div style={style}>Women's Page</div>;
};

export default Women;
