import React from 'react';

const Contact = () => {
  let style = {
    width: '100%',
    height: '30rem',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '3rem',
    textAlign: 'center',
  };
  return <div style={style}>Contact Page</div>;
};

export default Contact;
