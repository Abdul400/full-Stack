import React from 'react';

const About = () => {
  let style = {
    width: '100%',
    height: '30rem',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '3rem',
    textAlign: 'center',
  };
  return <div style={style}>About Page</div>;
};

export default About;
