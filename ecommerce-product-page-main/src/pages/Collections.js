import React from 'react';

const Collections = () => {
  let style = {
    width: '100%',
    height: '30rem',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '3rem',
    textAlign: 'center',
    zIndex: '1',
  };
  return <div style={style}>Collections Page</div>;
};

export default Collections;
