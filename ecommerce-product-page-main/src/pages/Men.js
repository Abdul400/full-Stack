import React from 'react';

const Men = () => {
  let style = {
    width: '100%',
    height: '30rem',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '3rem',
    textAlign: 'center',
  };
  return <div style={style}>Men's Page</div>;
};

export default Men;
